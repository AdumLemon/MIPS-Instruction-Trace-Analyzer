# MIPS-Instruction-Trace-Analyzer
A C-based command-line tool that analyzes MIPS instruction traces and generates execution statistics. It decodes R-, I-, and J-type instructions, tracks register usage, classifies memory operations, and evaluates branch behavior.
